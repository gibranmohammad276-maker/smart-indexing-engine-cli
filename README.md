# Smart Indexing Engine

Lightweight CLI tool for sitemap parsing, URL scoring, queue management, retry scheduling, and IndexNow automation.

Built for Termux and simple Python environments.

## Quick Start

```bash
pip install -r requirements.txt
python script.py
